import React from 'react';

class Chat extends React.Component {
  render() {
    return(
      <
    )
  }
}

Chat.defaultProps = {
  name: 'John',
};

Chat.propsTypes = {
  name: React.PropTypes.string,
};

export default Chat;
